name: synchronizers
description: The synchronizers component will keep a kafka topic synchronized with an external database.

synchronizers:
  - message: kafmesh.deviceId.customer
    type: protobuf
    partitions: 10

  - message: kafmesh.deviceId.customer
    type: protobuf
    partitions: 10
